# Numpy pour la lecture des fichiers
import numpy as np

f,FFTe,FFTs = np.loadtxt('FFTe_FFTs.txt',unpack=True)

# Avec le precedent PythonPhysique, detectez les maximas d'amplitude du signal 
# d'entree pour calculer le gain pour les frequences correspondant aux 
# differents pics

def GdB(FFTe,FFTs):
    return positions_maximas_locaux

liste_gains = ["Pas","ceci","Try","again"]

# Et l'affichage effectif des quatre premieres valeurs (attention, j'utilise 
# une pythonnerie...)
print(*liste_gains[:4])
